# Configuration des ports pour Docker Compose
# Modifiez ces ports pour éviter les conflits sur votre VPS
FRONTEND_PORT=3000
BACKEND_PORT=5001

# Chemin de la base de données (ne pas modifier sauf si nécessaire)
DB_PATH=/app/data/contest.db
